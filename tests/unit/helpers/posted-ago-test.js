import { postedAgo } from '../../../helpers/posted-ago';
import { module, test } from 'qunit';

module('Unit | Helper | posted ago');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = postedAgo(42);
  assert.ok(result);
});
