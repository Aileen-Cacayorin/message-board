import { postedDate } from '../../../helpers/posted-date';
import { module, test } from 'qunit';

module('Unit | Helper | posted date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = postedDate(42);
  assert.ok(result);
});
